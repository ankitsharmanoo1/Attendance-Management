# from msal import PublicClientApplication
# import config

# def authenticate():
#     app = PublicClientApplication(config.CLIENT_ID, authority=config.AUTHORITY)
#     result = app.acquire_token_interactive(config.SCOPES)
#     return result["access_token"]
